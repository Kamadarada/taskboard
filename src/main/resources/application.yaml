spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  application:
    name: api
  datasource:
    url: jdbc:postgresql://ep-summer-salad-a4j1tr1d-pooler.us-east-1.aws.neon.tech/verceldb?sslmode=require
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}

  jpa:
    hibernate:
      #      ddl-auto: create-drop
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: ${JWT_SECRET:dpakn1230rhokfnaowbd}

app:
  base-url: ${APP_BASE_URL:http://localhost:8080}

